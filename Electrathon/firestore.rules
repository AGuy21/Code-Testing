service cloud.firestore {
  match /databases/{database}/documents {
    // Matches all documents in the database
    match /{document=**} {
      // Allows anyone to read data without authentication
      allow read: if true;
      
      // Explicitly denies all write requests from client-side SDKs
      allow write: if false;
    }
  }
}    
